1. Выбрать в качестве функции \Phi(t,x) вариант из уравнения (13) в файле: 
\Phi(t,x)=L \max\left\lbracef(x)-t;0\right\rbrace+M P(x), L >0, M>0
2. Провести численное решение задачи 1 (пункты а, б, г, д) из файла
3. Выбрать в качестве функции \Phi(t,x) вариант из уравнения (32) в файле:
\Phi(t,x)=L|f(x)-t|+M P(x), L >0, M >0
4. Реализовать п. 2 с функцией \Phi(t,x) из п. 3 и сравнить результаты

Upd:
По графикам:

- отображаем на одном графике минимизируемую функцию, точку найденного приближения экстремума, область, описываемую ограничениями = > это график 1
- отображаем на втором графике линии уровня функции \Phi(t,x + область, описываемую ограничениями => это график 2

Т.е. если у нас идет работа сначала с функцией \Phi_1, потом с \Phi_2, то рисуем два набора графиков (всего 4 графика для каждой задачи): 
сначала график 1 для двух типов функций \Phi, потом график 2 для двух типов функций \Phi
